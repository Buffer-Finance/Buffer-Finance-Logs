{
  "name": "buffer-finance-logs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ Buffer-Finance/buffer-finance-logs",
    "create-local": "graph create --node http://localhost:8020/ Buffer-Finance/buffer-finance-logs",
    "remove-local": "graph remove --node http://localhost:8020/ Buffer-Finance/buffer-finance-logs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Buffer-Finance/buffer-finance-logs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
